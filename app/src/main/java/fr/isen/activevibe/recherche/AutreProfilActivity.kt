package fr.isen.activevibe.recherche

/*
import android.os.Bundle
import androidx.activity.ComponentActivity
import androidx.activity.compose.setContent
import androidx.compose.material3.MaterialTheme
import androidx.compose.material3.Surface
import fr.isen.activevibe.ui.theme.ActiveVibeTheme


class AutreProfilActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)

        // Récupérer le nom d'utilisateur depuis l'intent
        val username = intent.getStringExtra("nomUtilisateur") ?: "Utilisateur inconnu"

        // Utiliser setContent pour afficher notre UI Compose
        setContent {
            ActiveVibeTheme {
                // Lancer le composable AutreProfilScreen et lui passer le username
                Surface(color = MaterialTheme.colorScheme.background) {
                    AutreProfilScreen(username = username)
                }
            }
        }
    }
}

*/